services:
  - type: web
    name: akillab-ai-egitim-paneli
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENAI_MODEL
        value: gpt-5.2
