# Çekirdek (zorunlu)
pandas>=2.0
numpy>=1.24
scipy>=1.10
requests>=2.28

# Panel ekonometrisi
statsmodels>=0.14
linearmodels>=6.0

# Çok değişkenli / derin öğrenme
scikit-learn>=1.3
torch>=2.0            # LSTM için (yoksa analizi --no-dl ile çalıştırın)

# Veri çekme (opsiyonel)
wbgapi>=1.0           # World Bank WDI göstergeleri (build_panel.py)

# Dinamik panel (opsiyonel)
pydynpd>=0.2          # sistem-GMM; NumPy<2 ile en uyumlu (yoksa --no-gmm)

# Çıktı üretimi
matplotlib>=3.7       # şekiller
python-docx>=1.1      # Word tablosu
openpyxl>=3.1         # Excel okuma/yazma (edgar_fetch, csv_to_excel_safe)
pyarrow>=12.0         # parquet (opsiyonel)
